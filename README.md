# Fantasy Football Cheat Sheet

## How to run locally

1. In `/server` folder:
    npm install
    node server.js
2. In `/client` folder:
    npm install
    npm start

The frontend will open at http://localhost:3000  
The backend API runs at http://localhost:5000/api/players and fetches real player data from the Sleeper API.